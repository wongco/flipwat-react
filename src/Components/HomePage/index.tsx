import React, { PureComponent } from 'react';

class HomePage extends PureComponent {
  public render(): React.ReactNode {
    return <p>Choose a catgeory to get started!</p>;
  }
}

export default HomePage;
